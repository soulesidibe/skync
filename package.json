{
  "name": "skync",
  "version": "0.1.0",
  "description": "Track and sync skill folders vendored from git repos using a three-way merge.",
  "type": "module",
  "bin": {
    "skync": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "node-diff3": "^3.2.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
